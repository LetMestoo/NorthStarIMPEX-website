{
  "builds": [
    { "src": "package.json", "use": "@vercel/static-build" }
  ],
  "routes": [
    { "src": "/public/(.*)", "dest": "/public/$1" },
    { "src": "/services/(.*)", "dest": "/services/$1" },
    { "src": "/(.*)", "dest": "/$1" }
  ]
} 